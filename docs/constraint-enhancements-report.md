# 数据库约束增强实施报告

**日期**：2025-11-08
**状态**：✅ 已完成并通过测试

---

## 概述

针对高风险数据完整性问题，实施了以下数据库约束增强：

1. **默认版本唯一性约束**（数据库级）
2. **Namespace 删除保护**（应用层）
3. **Schema 删除保护**（应用层）
4. **SchemaField 约束验证**（应用层）
5. **JSONB GIN 索引**（性能优化）

---

## 实施详情

### 1. 数据库级约束（迁移：0002_add_database_constraints.py）

#### 1.1 GIN 索引（JSONB 查询优化）

```sql
CREATE INDEX IF NOT EXISTS idx_domain_extensions_gin
ON agent_cards
USING GIN (domain_extensions jsonb_path_ops);
```

**作用**：
- 加速 `domain_extensions` 的查询
- 支持 `domain_extensions ? 'schema_uri'` 查询
- 提升 "查找使用某个 Schema 的 AgentCard" 的性能

**测试结果**：✅ 已创建并存在

---

#### 1.2 部分唯一索引（默认版本约束）

```sql
CREATE UNIQUE INDEX IF NOT EXISTS idx_unique_default_version
ON agent_cards (namespace_id, name)
WHERE is_default_version = true;
```

**作用**：
- 数据库级保证每个 `(namespace, name)` 只有一个默认版本
- 防止应用层验证被绕过（如直接 SQL 操作）
- PostgreSQL 特性：部分索引（Partial Index）

**测试结果**：✅ 成功阻止创建第二个默认版本

---

### 2. 应用层保护（models.py）

#### 2.1 Namespace 删除保护

**位置**：`documents/models.py:62-72` (Namespace.delete())

```python
def delete(self, *args, **kwargs):
    card_count = self.agent_cards.count()
    if card_count > 0:
        raise ValidationError(
            f"无法删除命名空间 '{self.id}'：该命名空间下有 {card_count} 个 AgentCard。"
            f"请先删除或移动所有 AgentCard。"
        )
    super().delete(*args, **kwargs)
```

**保护场景**：
- 防止误删除生产环境的 Namespace
- 避免级联删除大量 AgentCard
- 要求用户先清理数据

**测试结果**：✅ 成功阻止删除，并提示 AgentCard 数量

---

#### 2.2 Schema 删除保护

**位置**：`documents/models.py:211-229` (SchemaRegistry.delete())

```python
def delete(self, *args, **kwargs):
    usage_count = AgentCard.objects.filter(
        domain_extensions__has_key=self.schema_uri
    ).count()

    if usage_count > 0:
        raise ValidationError(
            f"无法删除 Schema '{self.schema_type} {self.version}'："
            f"有 {usage_count} 个 AgentCard 正在使用此 Schema。"
            f"请先更新或删除这些 AgentCard。"
        )

    super().delete(*args, **kwargs)
```

**保护场景**：
- 防止删除被使用的 Schema（会导致 AgentCard 包含孤儿引用）
- 强制用户先迁移数据
- 利用 GIN 索引快速查询使用情况

**测试结果**：✅ 成功阻止删除，并提示使用数量

---

#### 2.3 SchemaField 约束验证

**位置**：`documents/models.py:340-394` (SchemaField.clean())

**验证规则**：

| 字段类型 | 允许的约束 | 禁止的约束 |
|----------|-----------|-----------|
| `integer` / `number` | `min_value`, `max_value` | `min_length`, `max_length`, `pattern`, `enum_choices` |
| `string` | `min_length`, `max_length`, `pattern` | `min_value`, `max_value`, `enum_choices` |
| `enum` | `enum_choices`（必填） | 其他所有约束 |
| `boolean` / `array` / `object` / `datetime` / `date` | 无 | 所有约束 |

**错误示例**：

```python
# ✗ 错误：为 integer 设置 min_length
field = SchemaField(field_type='integer', min_length=10)
# 错误信息：整数类型不能设置最小/最大长度，请使用最小/最大值。

# ✗ 错误：为 string 设置 min_value
field = SchemaField(field_type='string', min_value=10)
# 错误信息：文本类型不能设置最小/最大值，请使用最小/最大长度。

# ✗ 错误：enum 未设置 enum_choices
field = SchemaField(field_type='enum')
# 错误信息：枚举类型必须设置枚举值列表。
```

**测试结果**：✅ 所有验证规则正常工作

---

## 测试报告

### 测试方法

运行测试脚本：
```bash
docker-compose exec web python test_constraints.py
```

### 测试结果

```
============================================================
数据库约束测试
============================================================

=== 测试1：默认版本唯一性约束 ===
✓ 创建第一个默认版本成功
✓ 正确阻止第二个默认版本

=== 测试2：Namespace 删除保护 ===
✓ 正确阻止删除（提示1个AgentCard）
✓ 删除 AgentCard 后，成功删除 Namespace

=== 测试3：Schema 删除保护 ===
✓ 正确阻止删除（提示1个AgentCard正在使用）
✓ 删除 AgentCard 后，成功删除 Schema

=== 测试4：SchemaField 约束验证 ===
  ✓ integer 不能设置 min_length
  ✓ string 不能设置 min_value
  ✓ enum 必须设置 enum_choices

=== 测试5：GIN 索引检查 ===
✓ GIN 索引已创建（idx_domain_extensions_gin）
✓ 部分唯一索引已创建（idx_unique_default_version）

============================================================
测试结果汇总
============================================================
默认版本唯一性              ✓ 通过
Namespace删除保护        ✓ 通过
Schema删除保护           ✓ 通过
SchemaField约束验证      ✓ 通过
GIN索引                ✓ 通过

============================================================
✓ 所有测试通过！约束工作正常。
============================================================
```

---

## 数据库索引总览

### agent_cards 表的关键索引

| 索引名称 | 类型 | 字段 | 用途 |
|---------|------|------|------|
| `agent_cards_pkey` | PRIMARY KEY | `id` | 主键 |
| `agent_cards_namespace_id_name_version_uniq` | UNIQUE | `(namespace_id, name, version)` | Nacos唯一性 |
| **`idx_unique_default_version`** | **UNIQUE (Partial)** | **`(namespace_id, name)` WHERE `is_default_version=true`** | **默认版本唯一** |
| **`idx_domain_extensions_gin`** | **GIN** | **`domain_extensions`** | **JSONB 查询优化** |
| `agent_cards_namespa_1a663b_idx` | BTREE | `(namespace_id, name, is_default_version)` | 查询默认版本 |
| `agent_cards_namespa_8b4098_idx` | BTREE | `(namespace_id, is_active)` | 过滤激活的卡片 |
| `agent_cards_created_b20b65_idx` | BTREE | `created_at` | 按时间排序 |
| `agent_cards_updated_644f58_idx` | BTREE | `updated_at` | 按更新时间 |

---

## 数据完整性保障

### 保护机制矩阵

| 操作 | 约束级别 | 实现方式 | 可绕过方式 |
|------|---------|---------|-----------|
| **创建第二个默认版本** | 数据库 + 应用 | 部分唯一索引 + clean() | ❌ 无法绕过（数据库约束） |
| **删除有 AgentCard 的 Namespace** | 应用 | delete() 方法 | ⚠️  直接 SQL 可绕过 |
| **删除被使用的 Schema** | 应用 | delete() 方法 | ⚠️  直接 SQL 可绕过 |
| **字段约束类型不匹配** | 应用 | clean() 方法 | ⚠️  直接 SQL 可绕过 |

### 安全建议

1. **强约束**（已实现）：
   - 默认版本唯一性 → 数据库级保护 ✅

2. **中等约束**（已实现）：
   - Namespace/Schema 删除保护 → 应用层保护 ✅
   - 只要团队不直接操作 SQL，即可保证安全

3. **可选增强**（未来考虑）：
   - 添加数据库触发器（完全阻止直接 SQL 绕过）
   - 添加审计日志（记录所有删除操作）

---

## 性能影响

### GIN 索引查询性能

**测试场景**：查询使用特定 Schema URI 的 AgentCard

```python
# 查询使用某个 Schema 的所有 AgentCard
AgentCard.objects.filter(domain_extensions__has_key='https://my-org.com/schemas/physicalAsset/v1')
```

**性能对比**（预期）：

| 数据量 | 无索引 | 有 GIN 索引 | 提升 |
|--------|--------|------------|------|
| 100 条 | ~5ms | ~1ms | 5x |
| 1,000 条 | ~50ms | ~3ms | 16x |
| 10,000 条 | ~500ms | ~10ms | 50x |
| 100,000 条 | ~5s | ~30ms | 166x |

**注意**：小数据量时（<1000条），PostgreSQL 可能选择全表扫描而非索引。

---

## 用户影响

### Django Admin 用户体验

#### 1. 删除 Namespace 时

**之前**：直接删除，级联删除所有 AgentCard（危险）

**现在**：
```
无法删除命名空间 'prod'：该命名空间下有 127 个 AgentCard。
请先删除或移动所有 AgentCard。
```

#### 2. 删除 Schema 时

**之前**：直接删除，AgentCard 包含孤儿引用

**现在**：
```
无法删除 Schema 'physicalAsset v1'：有 42 个 AgentCard 正在使用此 Schema。
请先更新或删除这些 AgentCard。
```

#### 3. 创建第二个默认版本时

**之前**：允许创建（应用层验证）

**现在**：
```
Agent 'HPLC-001' 在命名空间 'dev' 已存在默认版本 '1.0.0'。
请先取消原默认版本。

（数据库级阻止，更安全）
```

#### 4. 字段约束设置错误时

**之前**：可以保存，但验证不生效

**现在**：
```
整数类型不能设置最小/最大长度，请使用最小/最大值。
```

---

## 迁移历史

| 迁移 | 操作 | 状态 |
|------|------|------|
| `0001_initial` | 创建所有模型 | ✅ 已应用 |
| `0002_add_database_constraints` | 添加 GIN 索引 + 部分唯一索引 | ✅ 已应用 |

### 回滚方法

如果需要回滚约束：

```bash
# 回滚到初始状态
docker-compose exec web python manage.py migrate documents 0001

# 重新应用约束
docker-compose exec web python manage.py migrate documents 0002
```

---

## 未来优化建议

### 已解决的问题 ✅

1. ✅ 默认版本可以重复（数据库约束已解决）
2. ✅ 删除 Namespace 导致数据丢失（删除保护已解决）
3. ✅ 删除 Schema 产生孤儿引用（删除保护已解决）
4. ✅ 字段约束配置错误（验证逻辑已解决）
5. ✅ JSONB 查询性能差（GIN 索引已解决）

### 可选的未来增强 📋

1. **乐观锁**（并发控制）
   - 添加 `version_number` 字段
   - 防止并发修改冲突

2. **审计日志**（可追溯性）
   - 记录所有创建/修改/删除操作
   - 使用 `django-auditlog` 或自定义日志

3. **软删除**（数据恢复）
   - 用 `is_deleted` 替代真删除
   - 支持"回收站"功能

4. **数据库触发器**（完全保护）
   - PostgreSQL 触发器阻止直接 SQL 绕过
   - 适合多系统共享数据库的场景

---

## 总结

### 完成的工作

✅ 添加数据库级默认版本唯一性约束
✅ 添加 Namespace 删除保护（应用层）
✅ 添加 Schema 删除保护（应用层）
✅ 添加 SchemaField 约束验证（应用层）
✅ 添加 JSONB GIN 索引（性能优化）
✅ 所有约束通过自动化测试
✅ 更新文档（database-constraints.md）

### 系统状态

🟢 **生产就绪**：所有高风险问题已解决
🟢 **性能优化**：JSONB 查询已优化
🟢 **数据安全**：关键约束已实施
🟢 **可维护性**：清晰的错误提示

### 下一步

可以安全地：
- 创建管理员账号
- 开始使用 Django Admin
- 创建 Namespace、Schema 和 AgentCard
- 部署到测试/生产环境

---

**报告完成日期**：2025-11-08
**测试状态**：✅ 5/5 通过
**生产就绪度**：🟢 就绪
