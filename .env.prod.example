# ============================================================
# 生产环境配置模板
# ============================================================
# 使用说明:
# 1. 复制此文件为 .env.prod:  cp .env.prod.example .env.prod
# 2. 修改下面的占位符为真实的生产环境值
# 3. 不要将 .env.prod 提交到 Git！
# ============================================================

# Django 配置
DJANGO_SECRET_KEY=REPLACE_WITH_RANDOM_50_CHAR_STRING
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=YOUR_SERVER_IP,YOUR_DOMAIN.COM,localhost,127.0.0.1

# Caddy 配置
# 无域名场景 (通过 IP 访问): CADDY_ADDRESS=:80
# 有域名场景 (自动 HTTPS): CADDY_ADDRESS=yourdomain.com
CADDY_ADDRESS=:80

# PostgreSQL 配置
POSTGRES_DB=agentcard_prod
POSTGRES_USER=produser
POSTGRES_PASSWORD=REPLACE_WITH_STRONG_PASSWORD
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Database URL (django-environ 自动解析)
DATABASE_URL=postgres://produser:REPLACE_WITH_STRONG_PASSWORD@db:5432/agentcard_prod

# ============================================================
# 生成随机密钥的方法：
# DJANGO_SECRET_KEY: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
# POSTGRES_PASSWORD: openssl rand -base64 32
# ============================================================
