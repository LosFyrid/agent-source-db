# ============================================================
# 测试环境配置模板
# ============================================================
# 使用说明:
# 1. 复制此文件为 .env.test:  cp .env.test.example .env.test
# 2. 修改下面的占位符为真实的测试环境值
# 3. 不要将 .env.test 提交到 Git！
# ============================================================

# Django 配置
DJANGO_SECRET_KEY=REPLACE_WITH_RANDOM_50_CHAR_STRING
DJANGO_DEBUG=True  # 测试环境可以开启 DEBUG（方便调试）
DJANGO_ALLOWED_HOSTS=YOUR_SERVER_IP,localhost,127.0.0.1

# Caddy 配置
# 测试环境通常使用 IP 访问，不需要域名
CADDY_ADDRESS=:80
CADDY_HTTP_PORT=8001     # 测试环境 HTTP 端口（默认8001，与生产环境8000区分）

# PostgreSQL 配置
POSTGRES_DB=agentcard_test
POSTGRES_USER=testuser
POSTGRES_PASSWORD=REPLACE_WITH_PASSWORD
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Database URL (django-environ 自动解析)
DATABASE_URL=postgres://testuser:REPLACE_WITH_PASSWORD@db:5432/agentcard_test

# ============================================================
# 测试环境说明：
# - 测试环境使用独立的数据库（与生产环境隔离）
# - 可以使用生产数据库的备份来还原测试数据
# - DEBUG=True 方便查看详细错误信息
# ============================================================
